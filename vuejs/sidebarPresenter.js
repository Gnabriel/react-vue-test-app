const SidebarPresenter = ({ model }) => {
  return (
    <SidebarView
      guests={"" /* TODO */}
      setGuests={
        "" /* TODO arrow function that calls the props.model  setNumberOfGuests() */
      }
    />
  );
};
